"use strict";//treat js code as newer version

let num = 874
let name = "myname"
let isTrue = false
// ek hota hai undefined like (let name;)
// or ek hota hai null which makes string null.
//object

console.log(typeof null);


// hum ek var b use kr skte hain but we don't because if there are two vars like var name = "name1" and other one is var name = "name2" then vo pure me var name2 ko hi krdega jaha b upar wala name likha hoga